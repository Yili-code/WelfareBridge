# 複製成 .env.local；docker-compose 的 web 服務以 env_file 讀取（不提交）。
# 本機（非 Docker）啟動的 Next.js 也讀同一個檔案。

# /admin 需求審閱的後台密碼。未設定時 /api/appeals 的 GET/PATCH 一律回 401。
# 請自行改成夠長的密碼，不要沿用這行範例值。
WELFARE_ADMIN_PASSWORD=change-me-to-a-long-random-password

# 只有在不使用 compose 的 backend 服務、要改連到別的後端時才需要設定。
# compose 內部預設為 http://backend:8000，一般不用動。
# BENEFIT_API_URL=http://127.0.0.1:8000
